{
  "name": "poetools",
  "version": "1.0.0",
  "description": "Auto update your synced filter on filterblade.xyz with Path of Exile",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . PoETools --platform=win32 --arch=x64 --ignore=/user_data --ignore=/.gitignore --prune"
  },
  "author": "Mateus Roscoe",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.1",
    "puppeteer": "^5.2.1"
  },
  "devDependencies": {
    "electron": "^9.1.1"
  }
}
